// In a config file or static property
const FACILITY_CONFIG = {
    "Garden": {
        subTypes: ["Decorative", "Food", "Herb", "Poison"],
        order: "Harvest"
    },
    "Workshop": {
        subTypes: ["Adventuring Gear", "Magic Item (Implement)"],
        order: "Craft"
    }
};

// Then simplify the logic:
const config = FACILITY_CONFIG[itemDoc.name];
if (config?.subTypes) {
    const options = config.subTypes.map(t => `<option value="${t}">${t}</option>`).join("");
    // ... render prompt
}
